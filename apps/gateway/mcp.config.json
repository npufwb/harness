{
  "servers": {
    "example-fs": {
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "description": "文件系统 MCP 服务（示例，按需启用）"
    }
  },
  "_comment": "配置外部 MCP 服务器，Gateway 启动时会连接并注册工具"
}
